# Paddle Client-Token (öffentlich, landet im Client-Bundle - kein Secret)
PUBLIC_PADDLE_CLIENT_TOKEN=test_xxx

# Paddle Environment: "sandbox" oder "production"
PUBLIC_PADDLE_ENVIRONMENT=sandbox

# Optional: Analytics/Tracking (falls später ergänzt)
# PUBLIC_PLAUSIBLE_DOMAIN=humanshield-awareness.de